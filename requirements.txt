# ── Core (Week 2+) ────────────────────────────────────────────────────────
anthropic>=0.40.0            # Claude SDK — Intake, Matching, Negotiation, Monitor
pydantic>=2.5                # LeadState typed schema (Week 2)
python-dotenv>=1.0.0
pyyaml>=6.0

# ── Data + warehouse (Week 1+) ────────────────────────────────────────────
duckdb>=1.0                  # direct DuckDB reads from src/data/warehouse.py
dbt-core>=1.11,<1.12
dbt-metricflow>=0.13,<0.14
dbt-duckdb>=1.10,<1.11

# ── LangGraph orchestration (Week 3-4) ────────────────────────────────────
langgraph>=0.2               # state machine, native interrupt/resume for HITL
langgraph-checkpoint-sqlite>=2.0   # the SqliteSaver checkpointer

# ── MCP servers (Week 3) ──────────────────────────────────────────────────
mcp>=1.0                     # Model Context Protocol — partner_capacity, slack, crm

# ── Observability (Week 5) ────────────────────────────────────────────────
# langfuse>=2.30             # OPTIONAL: live forwarding — the local
#                              TraceRecorder needs no server or SDK

# ── UI (Week 6) ───────────────────────────────────────────────────────────
streamlit>=1.40              # demo + manager HITL console
pandas>=2.0                  # dashboard rendering

# ── Tests ─────────────────────────────────────────────────────────────────
pytest>=8.0
